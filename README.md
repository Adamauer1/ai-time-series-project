# ai-time-series-project

Create environment - python -m venv venv

Activate environment - venv\Scripts\activate

Save pip mods - pip freeze > requirements.txt

Load pip mods - pip install -r requirements.txt
